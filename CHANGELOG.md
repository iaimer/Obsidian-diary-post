# 更新日志

所有重要的更改都将记录在此文件中。

## [0.12.0] - 2026-06-03

### Android 自用 APK 与离线同步
- **Capacitor Android 容器**：将 Web App 打包为「荔枝日记」Android APK，英文名 **Litchi Journal**，包名 `com.iaimer.litchijournal`
- **项目英文标识**：package name 更新为 `litchi-journal`，GitHub 建议名为 `litchi-journal`
- **首次配置引导**：原生环境无 Token 时显示 API 地址（预填）和 Token 填写页，测试连接通过后进入主页
- **平台自动识别**：`isNativeApp()` 检测 Capacitor 原生平台，自动启用远程模式、隐藏本地 Vault 连接按钮
- **离线 Outbox**：IndexedDB 新增 `outbox` 表，远程模式下文字记录、习惯更新、图片上传先入队再同步
- **习惯合并**：同一日期新 `update_habits` 入队时自动删除旧操作，仅保留最新状态
- **自动同步触发器**：App 启动/回到前台/网络恢复时自动调用 `syncPending()`，内部锁防止并发
- **同步状态条**：顶部显示离线/同步中/失败+重试/完成 4 种状态，仅远程模式展示
- **服务端幂等**：写入 API 新增 `operationId` 校验，通过 `assets/.diary-ops.json` 旁路索引防重复，不污染日记正文
- **创建日记幂等**：`POST /create` 文件已存在时返回成功而非 400 错误
- **上海时区工具**：前端新增 `getShanghaiDateString()` 和 `getShanghaiYearMonth()`，确保日期不受设备时区影响
- **离线跨日修复**：文字和习惯队列保存记录时的上海日期，文字额外保存原始时间戳，次日补同步仍写入原日记
- **中断恢复**：App 重启时将遗留 `syncing` 操作恢复为 `pending`，避免进程终止造成待同步记录永久卡住
- **图片离线队列**：相册图片压缩后以 Blob 写入 outbox，同步成功后删除；写日记失败时服务端清理孤立图片
- **Android 安全与交互**：关闭应用数据备份，补充系统返回键、安全区和 Capacitor listener 清理
- **应用图标**：替换 Android launcher 图标为荔枝日记专属图标
- **润色解析修复**：AI 偶发返回不规范标签格式时，不再误判“润色结果为空”
- **版本显示同步**：Android `versionName` 与设置页版本号同步为 `0.12.0`

### 依赖新增
- `@capacitor/core@8.3.4`、`@capacitor/android@8.3.4`、`@capacitor/cli@8.3.4`、`@capacitor/app@8.1.0`、`@capacitor/network@8.0.1`

### 新增文件
- `src/utils/platform.ts` — 原生平台检测
- `src/components/FirstTimeConfig.tsx` — 首次 Token 配置页
- `src/services/outboxService.ts` — 离线队列服务
- `src/components/SyncStatusBar.tsx` — 同步状态条
- `capacitor.config.ts` — Capacitor 容器配置
- `public/icons/lychee_diary_icon_flat.svg` — Android launcher 图标源文件
- `android/` — Android 原生工程目录

## [0.11.1] - 2026-05-31

### 安全修复
- **远程 API 凭据隔离**：移除仓库和前端中的默认 Token，`server/config.json` 改为部署机器本地配置，新增 `server/config.example.json`
- **图片读取目录穿越防护**：限制图片文件名格式，并校验解析后的路径始终位于目标 `assets` 目录
- **日记 HTML 注入防护**：日记内容渲染前先转义，仅保留受控的加粗标签
- **部署信息脱敏**：部署文档和进度文档不再记录真实 Token、本地路径或内网地址

### 稳定性修复
- **上海时区统一**：服务端新增统一日期工具，日记路径、模板、历史记录和统计均按 `Asia/Shanghai` 归档
- **图片上传顺序修复**：上传前先确认目标日记存在，避免失败请求留下孤立图片

### 部署提醒
- 历史 Token 已失效处理前不得继续使用；部署时需复制 `server/config.example.json` 为本地 `server/config.json`，并填写新的随机 Token

## [0.11.0] - 2026-05-24

### Dock 重构
- **FAB 嵌入导航栏**：添加记录主入口移至 dock 中央（第 3 个位置），5 项均匀分布
- **四步焦虑引导**：点击焦虑图标依次回答 4 个问题，统一写入 `😰 焦虑时刻` 区块
- **焦虑区块渲染**：日记预览和详情页新增焦虑时刻展示（橙色边框 + 斜体答案）
- **焦虑区块自动隐藏**：无真实内容时不在首页和过往页显示
- **非今天页面 FAB 行为优化**：点击记录按钮先切回今天页面再弹窗

### 子按钮图标更新
- 小确幸 → 粉红爱心 SVG
- 随手记 → 铅笔 SVG
- 觉察 → 青色云朵气泡 SVG
- 焦虑时刻 → 黄色忧虑脸 SVG

### 标签系统重构
- **提取 TAG_SYSTEM 共享配置**：`src/config/tags.ts` 统一管理，3 个 Modal 改为 import
- **新增 `#生活` 领域**：7 个领域标签，含 5 个能力标签（健康管理、财务管理、生活整理、兴趣探索、日常记录）
  - `#尝鲜种草` 合并入 `#兴趣探索`
- **新增 `#回忆` 方法标签**：方法层扩展至 5 个

### 布局优化
- **页面宽度扩展**：主页/过往页 `max-w-md` → `max-w-2xl`（448→672px），统计页 → `max-w-3xl`（768px），设置页 → `max-w-xl`（576px），大幅减少侧向空白
- **header→首模块间距统一**：过往页/设置页从 24px 改为 16px，与统计页保持一致
- **统计页底色统一**：`bg-gray-100` → `bg-gray-50`，与其余页面一致
- **日历选中颜色优化**：深咖啡色 `bg-indigo-600` → 浅橙色 `bg-orange-100` + ring 圆环，与日历整体色调更协调

### AI 润色改进
- **领域优先级重排**：`#生活` 从第 6 位提到第 2 位，明确睡眠/健康/情绪/饮食归入生活
- **新增润色示例**：`半夜醒来翻来覆去睡不着。 #生活 #健康管理 #记录`
- **同步更新 `DEFAULT_POLISH_PROMPT`**：标签列表与 `tags.ts` 保持一致

### 日记路径更新
- **Obsidian 日记目录变更**：`workspace/生活/日记/` → `01.日记/`，同步更新 `fileSync.ts` 和 `historyService.ts` 中所有路径引用

### Bug修复
- **焦虑内容写入错误区块**：`appendToSection` 未识别 `## 📈 每日复盘` 为区块边界，修复前后端
- **blockquote 颜色渲染错误**：引用块渲染一直误用 happiness 配色，修复为使用正确 section 配色
- **时间戳重复**：焦虑引导不自带时间，由 `formatAnxiety` 统一处理
- **测试数据残留**：清理日记文件中的测试条目
- **习惯编辑弹窗被遮挡**：`PullToRefresh` 的 `transform` 导致 `fixed` 定位失效，改用 `createPortal` 渲染到 `document.body`
- **数字输入框前导零残留**：`type="number"` 对前导零行为异常，改为 `type="text" inputMode="numeric"` 手动过滤
- **所有页面统一 flex 布局**：主页/统计/过往/设置全部改为 `flex flex-col h-screen + flex-1 overflow-y-auto`，header 不再遮挡内容，滚动正常
- **标签与文字左对齐**：标签移入 `flex-1 min-w-0` 容器，与记录文字对齐，修复 `ml-8` 侵入时间戳区域
- **中英文混排右侧锯齿**：内容改用 `div` 块级元素 + `text-justify` 两端对齐
- **上传照片支持取消**：拆分为压缩/保存/引用独立步骤，每步检查取消标记；远程模式使用 AbortController 中断 fetch
- **上传按钮动效**：上传中显示 spinner + "取消上传"，可点击取消
- **AI 润色结果为空时覆盖原文**：`handlePolish` 增加空结果校验，`handleUsePolished` 保护原始内容不被空结果覆盖
- **缓存提示词过期问题**：新增 `isPromptStale()` 检测 localStorage 中缓存的旧 prompt（缺 `#生活`/`#回忆`），自动降级到默认

### 部署
- **Vite 允许 Cloudflare Tunnel 转发**：`server.allowedHosts` 添加 `obsidian.femkits.org`
- **Cloudflare Tunnel**：稳定运行于 PID 81392，双服务（前端 4000 + API 4001）

## [0.9.0] - 2026-05-23

### UI重新设计
- **色彩系统重建**：暖灰底色 + 陶土色强调色，替换冷灰/AI紫蓝
  - 全局 gray 色板替换为暖灰色系
  - indigo 色板替换为陶土色系（terracotta）
  - `tailwind.config.js` 自定义颜色覆盖，无需改组件代码
- **字体优化**：中文字体栈 PingFang SC → Hiragino Sans GB → Noto Sans SC
  - 添加字体抗锯齿渲染
  - 全局平滑滚动
- **布局去卡片化**：淡化白色卡片阴影，改用柔和边框分隔
  - 头部/导航改为毛玻璃效果（backdrop-blur）
  - 增加页面呼吸感，统一各页面底部间距
- **交互动效**：所有模态框添加 scale+fade 入场动画
  - 全局按钮按压反馈（active: scale 0.97）
  - Loading 状态从纯文字改为 spinner 动画
  - 可复用 skeleton 组件类
- **日记模块彩色竖线系统**：7个模块用彩色左边框区分，暖→冷渐变
  - ✍️ rose → ✨ amber → 😰 orange → 💡 emerald → 🧠 teal → 🌙 sky → 📸 violet
  - 时间戳颜色与模块边框一一对应
  - 标签统一低对比灰色，不抢眼
- **过往页面同步**：DiaryDetail 与首页日记显示样式完全一致
- **日历配色修复**：蓝色系"有日记"标记改为琥珀暖色
- **删除死代码**：QuickInput.tsx（411行无用代码）
- **FAB 图标更新**：主按钮从铅笔换为笔记本 SVG

## [0.8.0] - 2026-05-23

### 新增功能
- **悬浮快速记录按钮**：主页右下角新增悬浮按钮，整合快速记录入口
  - 点击主按钮展开三个子按钮：随手记、觉察、小确幸
  - 子按钮向左上方展开，点击后打开对应弹窗
  - 展开时显示关闭图标，点击遮罩或主按钮收起
- **SVG图标系统**：悬浮按钮使用精美 SVG 图标
  - 主按钮：铅笔图标（writing-pencil）
  - 随手记：备忘录图标（memo）
  - 觉察：思考表情图标（thinking-face）
  - 小确幸：心形图标（heart）

### UI优化
- **主页精简**：移除原有的 QuickInput 组件和觉察/小确幸按钮，页面更简洁
- **随手记弹窗化**：原嵌入组件改为弹窗形式，点击悬浮按钮打开

## [0.7.4] - 2026-05-23

### Bug修复
- **修复AI API JSON解析错误**：当API返回HTML错误页面时不再抛出"N is not valid JSON"，改为显示中文错误提示
- **修复明日寄语重新生成不刷新**：重新生成人生教练时同步替换明日寄语区块，不再出现旧行动建议残留叠加

### 部署改进
- **PM2配置可移植**：`ecosystem.config.cjs` 使用 `__dirname` 替代硬编码路径，适配不同机器

## [0.7.3] - 2026-05-22

### 新增功能
- **过往页面自动预览昨天日记**：打开过往页面自动展开昨天的日记详情
- **日历选中日期高亮**：正在查看的日记日期显示 indigo 背景高亮

### 功能改进
- **日历小圆点显示标准**：改为检查是否有随手记/小确幸/觉察/焦虑时刻内容，忽略习惯追踪模块
- **今天标记样式调整**：浅 indigo 背景 + 小点标记，不再全背景高亮
- **待开发功能规划**：PLAN.md 新增已完成和待开发功能清单

## [0.7.2] - 2026-05-22

### UI优化
- **生产环境隐藏远程模式提示**：主页和过往日记页面右上角不再显示"远程模式"标签
- **导航栏文案优化**：Dock"历史"改为"过往"，页面标题"历史日记"改为"过往日记"
- **已完成习惯字体颜色**：保持灰色而非白色，提高可读性

## [0.7.1] - 2026-05-22

### 新增功能
- **🏃 习惯管理功能**：设置页面支持添加、编辑、删除习惯
  - 习惯配置：名称、图标、类型（数值型/勾选型）、目标值、单位、描述、启用状态
  - 动态渲染：HabitTracker 根据配置动态显示习惯
  - 统计页面：热力图标签从布尔型习惯配置动态生成
- **🎨 自定义背景颜色**：每个习惯可选择独立的背景颜色
  - 10种颜色：蓝、天蓝、绿、翠绿、橙、琥珀、紫、紫罗兰、粉、玫瑰
  - 默认配置：饮水蓝色、运动绿色、阅读橙色、语言紫色、补充剂粉色
- **😀 分组emoji选择器**：点击图标弹出分组滚动面板
  - 6个分组：常用、生活、食物、学习、活动、表情
  - 滚动面板限制高度48px，避免页面拉长
- **SVG图标按钮**：设置页面习惯管理使用统一SVG图标
  - 启用按钮：circle-check（启用）/ circle-x（禁用）
  - 编辑按钮：edit-pencil
  - 删除按钮：trash-can

### 变更
- 状态持久化新增 `habitConfigs`
- 日记格式化使用动态配置 `formatHabitData(habitData, configs)`
- 统计计算使用动态目标值 `calculateSummary(stats, configs)`
- HabitConfigEditModal 标签改为"图标"（后期支持自定义图标）

## [0.7.0] - 2026-05-22

### 新增功能
- **🧠 人生教练（原荔枝喵说）**：基于当天日记内容生成 AI 教练式反馈
  - 点击卡片内「生成今日教练反馈」按钮，自动收集所有日记区块内容发送给 AI
  - 输出包含 📌 模式识别 / ⚠️ 矛盾指出 / 💬 暖心鼓励（250-300 字）
  - 有内容时标题右侧显示「🔄 重新生成」按钮
- **教练提示词自定义**：设置页 AI 润色引擎内新增「教练提示词」Tab，与润色规则并排切换
- **明日寄语卡片**：在人生教练与影像记录之间显示当日明日寄语内容
- **🎯 行动建议分离**：AI 输出的行动建议自动提取并追加到日记的明日寄语区块
- **向后兼容**：旧日记的 `🧠 荔枝喵说` 标题自动识别，下次写入时迁移为 `🧠 人生教练`

### 变更
- 新日记模板生成 `🧠 人生教练` 区块标题（替代旧的 `🧠 荔枝喵说`）
- 版本号 `0.6.9` → `0.7.0`

## [0.6.9] - 2026-05-21

### 新增功能
- **SVG 图标系统**：Dock 导航、页面标题、添加照片按钮均使用统一 SVG 图标
  - 今天、历史、统计、设置四个导航项
  - 页面标题与导航栏图标保持一致
  - 添加照片按钮使用相机图标
- **主题切换图标**：设置页右上角点击图标循环切换深色/浅色/跟随系统
  - 深色模式：月亮图标
  - 浅色模式：太阳图标
  - 跟随系统：图标右下角蓝色 `A` 标识
  - 移除原来的折叠式显示设置

### Bug修复
- **当天无日记时习惯数据未重置**：切换到无日记的日期时饮水/步数等回退到默认值

## [0.6.8] - 2026-05-20

### 新增功能
- **习惯打卡纵向布局**：一行一个习惯，改善手机端显示效果
  - 饮水/运动显示详细信息（名称、目标值、当前值）
  - 勾选类习惯使用圆形复选框样式
- **饮水/运动进度条背景**：
  - 饮水：浅蓝色背景 + 蓝色进度条
  - 运动：浅绿色背景 `#DDFFE4` + 绿色进度条 `#ADEBB3`
- **远程环境自动启用远程模式**：访问远程地址时无需手动勾选
  - 自动配置 API 地址和 Token
  - 自动启用远程模式
- **API Token 显示/隐藏切换**：设置页面添加切换按钮

### 功能改进
- **端口调整**：3000/3001 → 4000/4001，避免与常见开发端口冲突
- **运动步数输入简化**：移除快捷按钮，仅保留直接输入框
- **移除饮水杯子emoji**：避免手机端换行导致模块高度不一致
- **API 地址自动清理**：保存时移除末尾 `/api/v1` 防止路径重复

### Bug修复
- **远程API认证问题**：修复 API 地址格式错误导致请求失败
  - 测试连接使用正确的 API 端点 `/api/v1/diary/{date}`
  - 测试连接成功后自动启用远程模式
  - 生产环境自动配置远程 API 地址

### 部署优化
- **PM2 双服务部署**：ecosystem 配置支持前端和 API 同时运行
  - `diary-api`：Express API 服务（端口 4001）
  - `diary-app`：`serve` 托管前端构建产物（端口 4000）

## [0.6.7] - 2026-05-15

### 新增功能
- **快速记录文本框自适应**：输入框随文字增多自动扩展高度，最大高度 200px
- **觉察和小确幸添加时间戳**：与随手记统一 `**HH:MM**` 时间戳格式
  - 觉察：`- **HH:MM** 内容 #标签`
  - 小确幸：`> **HH:MM** 内容 #标签`
  - 前端本地和服务端远程模式均已支持
- **觉察标签独立样式**：时间戳黄色粗体、标签黄色圆角 `bg-yellow-100 text-yellow-700`
- **小确幸标签独立样式**：引用块提取 `#` 标签并渲染为绿色圆角 `bg-green-50 text-green-600`
- **习惯趋势图改为柱状图**：折线图替换为柱状图，蓝色饮水柱 + 橙色运动柱，顶部圆角
- **习惯达标虚线**：饮水 1500mL 和运动 6000 步达标参考线，对应颜色虚线

### 功能改进
- **小确幸样式统一**：去除左侧竖线和斜体，与觉察/随手记保持统一

### Bug修复
- **觉察和小确幸保存时标签丢失**：修复 handleSubmit 未将选中标签传递给 DataService 的问题
  - DataService 接口及 Local/Remote 实现全部添加 tags 参数
  - fileSync 层添加 tags 格式化支持
  - 服务端 happiness/reflection 路由支持 tags 参数
  - DiaryView 和 DiaryDetail 渲染同步更新

### 文档更新
- **关于页**：荔枝喵说状态更新为"APP 内 AI 生成（待实现）"

## [0.6.6] - 2026-05-15

### 新增功能
- **图片上传与浏览器端压缩**：支持从手机/电脑上传照片到日记影像记录
  - Canvas API 浏览器端压缩，长边最大 2000px，JPEG 质量可调
  - 压缩目标 < 2MB，逐步降质量 + 降分辨率兜底
  - 上传后自动写入 Vault assets 目录并追加 WikiLink 引用
- **远程模式图片上传**：手机端通过 API Server 远程上传
  - base64 编码传输，服务端写入 assets + 追加 WikiLink
- **图片压缩设置**：可自定义压缩参数
  - 最大长边 (px)、最大文件大小 (MB)、JPEG 质量
  - 自定义文件名格式（占位符 `{date}` `{seq}`）
  - 设置持久化到 localStorage
- **今日日记图片大图查看**：点击影像记录缩略图打开 ImageModal 全屏弹窗
  - 与历史页面一致的交互体验

### 功能改进
- **日历格子优化**：记录数文字替换为日期下方蓝色小点
- **上传按钮**：右对齐，"添加照片"文字，无 emoji
- **图片命名**：`Image-YYYYMMDD-序号.jpg` 格式，自动扫描避免重名

### Bug修复
- **日历今天有图片时日期数字不显示**：修复 isToday + imageUrl 时缺少 text-white 样式
- **影像记录刷新问题**：上传后通过 triggerRefresh 机制确保 DiaryView 重新加载

## [0.6.5] - 2026-05-14

### 新增功能
- **觉察和小确幸润色**：与随手记统一润色功能
  - 添加润色按钮、标签选择器、AI标签显示
  - 支持三层标签体系（领域+能力+方法）
  - 润色预览、继续编辑、取消功能
- **自定义润色规则**：设置页面可自定义润色提示词
  - 默认润色规则：尊重事实零增补 + 适度修辞 + 轻微扩写
  - 可编辑 textarea 自定义规则内容
  - 重置为默认规则按钮
- **统一润色规则**：随手记、觉察、小确幸使用相同润色规则
  - 尊重事实零增补：不添加未提及的人物、事件、地点
  - 适度修辞：可适当用比喻、拟人，基于原文信息处理
  - 轻微扩写：可做1-2句扩写，仅延伸原文氛围/情绪

### 功能改进
- **ReflectionModal**：添加润色功能、标签选择器、标签显示
- **HappinessModal**：添加润色功能、标签选择器、标签显示
- **SettingsPage**：添加润色规则自定义 textarea

---

## [0.6.4] - 2026-05-14

### 新增功能
- **设置页面折叠分组**：提升美观度，便于后续扩展设置项
  - 新增 CollapsibleSection 可复用折叠组件
  - 远程API设置、AI润色引擎默认折叠
  - 关于分组保持展开状态
  - 点击标题栏展开/折叠，箭头图标旋转动画
- **日记不存在时新建按钮**：检测日记文件是否存在，不存在显示新建按钮
  - DiaryView 先检查文件是否存在
  - 不存在时显示"新建日记"按钮
  - 点击按钮创建日记模板文件
  - 写入操作（随手记、习惯等）需要日记已存在，避免自动创建混乱历史

### API变更
- 服务端新增 `/api/v1/diary/exists/:date` - 检查日记是否存在
- 服务端新增 `/api/v1/diary/create` - 创建日记文件
- 写入操作不再自动创建日记文件，需先调用 create

---

## [0.6.3] - 2026-05-12

### Bug修复
- **历史界面影像记录残留**：切换到无图片日期时，清空旧的图片状态
- **底部导航遮挡内容**：调整页面底部间距为 50px，刚好适配导航栏高度

---

## [0.6.2] - 2026-05-12

### Bug修复
- **AI润色能力标签被清空**：修复批量设置标签时 useEffect 过早清空能力标签的问题
  - 添加 `isBatchSettingTags` 标记，批量设置时跳过清空逻辑
  - 使用 `setTimeout` 延迟重置标志，确保 useEffect 执行完毕

---

## [0.6.1] - 2026-05-12

### 新增功能
- **远程模式完整支持**：
  - HistoryPage 支持远程 API（/api/v1/history/:year/:month）
  - HabitStats 支持远程 API（/api/v1/stats/habit）
  - CalendarDayCell/DiaryDetail 远程模式图片加载
  - 服务端新增图片 API（/api/v1/diary/image/:year/:imageName）

### 功能改进
- **习惯统计实时更新**：更新习惯后自动触发统计刷新
- **样式统一优化**：
  - DiaryDetail 与 DiaryView 模块颜色区分保持一致
  - 小确幸：bg-green-50 圆角矩形
  - 觉察与迭代：bg-yellow-50 圆角矩形
  - 荔枝喵说：渐变背景圆角矩形
  - 与 dock 卡片样式区分开
- **日历缩略图简化**：移除相机 emoji 标记，直接显示图片

### Bug修复
- **小确幸显示问题**：统一 DiaryDetail 和 DiaryView 过滤逻辑，排除 callout 标记
- **远程模式历史页面**：正确处理 vaultConnected 和 remoteMode 状态判断

---

## [0.6.0] - 2026-05-12

### 新增功能
- **多设备同步**：手机 + Mac 远程访问方案
  - Express API Server（TypeScript + ESM）
  - Token 认证中间件
  - 日记/习惯/历史 API 接口
  - DataService 抽象层（Local + Remote）
  - 远程模式配置界面

### 功能改进
- **DiaryView 布局优化**：
  - 统一圆角卡片样式
  - 分隔线左右留边距
  - 间距紧凑统一
- **设置页面**：
  - 新增"远程API设置"区块
  - API地址 + Token 配置
  - 测试连接按钮 + 状态反馈

### Bug修复
- **Markdown 解析标题不一致**：server 端 `焦虑时刻` 标题匹配
- **DiaryView 远程模式未加载**：添加 remoteMode 检查
- **HH:MM 占位符未删除**：server 端 appendToSection 修复

### 部署优化
- **pm2 进程管理**：
  - 崩溃自动重启
  - 日志分离（error.log + out.log）
  - 时间格式化
- **一键部署脚本**：`deploy.sh`
- **Mac mini 部署指南**：`DEPLOY.md`

---

## [0.5.1] - 2026-05-12

### 功能改进
- **下拉刷新体验优化**：
  - 添加 `overscroll-behavior: contain` 阻止页面级弹性滚动干扰
  - 降低触发阈值从80px到50px，更容易触发
  - 添加刷新成功提示（绿色横幅"✓ 刷新成功"，1.5秒后消失）
  - 历史页面支持下拉刷新功能
  - 统一布局模式：header和内容放入PullToRefresh内
  - 底部导航添加 z-50 防止遮挡

### Bug修复
- **DiaryView时区问题**：`toISOString`改为`getDateString`，使用本地时间而非UTC
- **代码清理**：移除未使用的 `diary.ts` 服务和 `parseDateString` 函数

### 新增
- **AGENTS.md**：项目约束文档，帮助OpenCode等AI工具快速理解项目

---

## [0.5.0] - 2026-05-11

### 新增功能
- **历史日记查看页面**：完整的月视图日历和日记详情
  - 月视图日历组件，支持月份切换
  - 有日记的日期显示蓝色标记
  - 有图片的日期显示缩略图背景
  - 当前日期高亮显示
  - 点击日期查看日记详情（不含习惯追踪）
- **影像记录缩略图**：日历格子显示图片缩略图
  - 图片存储支持月份assets和年份assets两种路径
  - 图片URL缓存机制，避免重复加载
  - 渐变遮罩层确保日期数字清晰可见
  - 加载状态和错误处理提示
  - 平滑过渡动画和交互效果
- **图片放大查看**：全屏弹窗查看大图
  - 点击图片缩略图放大显示
  - 支持左右箭头切换多图
  - 键盘操作（ESC关闭，箭头切换）
  - 超过3张图片显示底部缩略图导航
  - 点击背景或关闭按钮关闭弹窗

### Bug修复
- **图片加载路径问题**：修复月份assets文件夹图片无法显示
  - 图片可能存储在月份assets（MM.EnglishMonth/assets）
  - 或年份assets（YYYY/assets）
  - 按优先级查找：月份assets > 年份assets

### 功能改进
- **底部导航优化**：添加"历史"按钮，统一导航切换逻辑
- **日记详情优化**：影像记录图片网格显示，hover放大效果
- **性能优化**：图片缓存机制，提升加载性能

---

## [0.4.0] - 2026-05-10

### 新增功能
- **自动创建日记文件**：当天日记不存在时自动创建
  - 符合Obsidian模板格式的完整日记结构
  - 打开App、添加随手记、更新习惯时均可触发
  - 路径格式：workspace/生活/日记/YYYY/MM.EnglishMonth/YYYY-MM-DD.md

### Bug修复
- **时区问题**：修复UTC日期与本地日期不一致
  - `getDateString`改用本地时间，确保Asia/Shanghai时区正确
  - 修复凌晨时段日记写入错误日期文件的问题
- **阅读模式增强**：
  - 荔枝喵说区块显示阅读模式，不显示Markdown标记
  - 解析时跳过`---`分隔线，避免显示在区块末尾
  - 过滤随手记模板示例`- **HH:MM** 内容 #标签`
  - 小确幸过滤空引用行`> `
- **区块显示优化**：无内容的区块不显示（小确幸、觉察、荔枝喵说）

### 功能改进
- **添加随手记时清理模板**：自动删除日记模板中的格式示例行
- **固定开发端口**：vite服务器固定端口3000，避免localStorage数据因端口变化丢失

---

## [0.3.0] - 2026-05-09

### 新增功能
- **习惯统计页面**：近30天习惯数据可视化
  - 趋势图：饮水+运动双Y轴折线图（显示近7天，统计基于30天）
  - 热力图：阅读/学语言/补充剂标签式切换（微信读书风格）
  - 渐变绿色：连续打卡越久颜色越深
  - 自动从Obsidian文件读取历史数据并缓存
- **AI润色校验加强**：
  - 提示词明确要求两层必选标签（领域+能力）
  - 缺少能力标签时显示警告并禁用发送按钮

---

## [0.2.0] - 2026-05-09

### 新增功能
- **设置页面**：支持完全自定义API配置
  - 服务名称自定义
  - Base URL自定义（支持任意API地址）
  - 模型名称自定义
  - API Key输入（支持显示/隐藏）
- **预设模板**：一键填充常用API配置
  - Claude API
  - OpenAI API
  - DeepSeek
  - Moonshot
  - 本地Ollama
- **AI润色引擎**：自动润色内容并添加标签
  - 润色规则：生动化扩写、事实零增补、拒绝代写总结、口语化、简短有节奏
  - 自动根据标签规范添加三层标签（领域+能力+方法）
  - 支持Claude API和OpenAI兼容API两种格式

### 功能改进
- **习惯追踪**：快捷按钮改为累加模式
  - 饮水：-250/+250/+500/目标/清零
  - 运动：-1000/+1000/+3000/目标/清零

---

## [0.1.0] - 2026-05-09

### 首次发布

#### 核心功能
- **随手记输入**：快速记录日常事件
  - 三层标签选择器（领域+能力+方法）
  - 自动获取时间戳
  - 直接追加到Obsidian日记文件
- **觉察与小确幸**：快速输入弹窗
  - 觉察区块：追加到"💡 觉察与迭代"
  - 小确幸区块：追加到"✨ 每日小确幸"
- **习惯打卡**：5项习惯追踪
  - 饮水(mL)、运动(步)、阅读、学语言、补充剂
  - 目标提示：饮水≥1500mL、运动≥6000步为达标
- **日记查看**：分区块展示
  - 随手记、小确幸、觉察、荔枝喵说
  - 阅读模式渲染（无Markdown标记）
  - 标签显示在记录下方（中性灰颜色）

#### 技术特性
- **Obsidian Vault集成**：File System Access API
  - 路径格式：YYYY/序号.英文月份名/YYYY-MM-DD.md
  - 安全追加模式：先读再追加，不覆盖
- **离线缓存**：IndexedDB (Dexie.js)
- **自动刷新**：添加记录后自动更新显示
- **状态持久化**：localStorage保存连接状态和习惯数据

#### 技术栈
- React + TypeScript + Vite
- Tailwind CSS
- Zustand状态管理
- Dexie IndexedDB
